
export const Main = ({
    children
    }:{
        children:React.ReactNode
    }) => {
    return (
        <div className=" max-w-c ml-auto mr-auto">
            {children}
        </div>
    )
}
