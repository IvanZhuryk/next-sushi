
export const Main = ({
    children
    }:{
        children:React.ReactNode
    }) => {
    return (
        <main className=" 2xl:w-1536  ml-auto mr-auto">
            {children}
        </main>
    )
}
