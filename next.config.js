

/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns:[
            {
                hostname:"sushi-pizza39.com.ua"
            }
        ]
    },
}

module.exports = nextConfig
